{
  "schemaVersion": "0.8.0",
  "description": "Canonical puzzle/solution catalog plus solver retrieval, replay-backed fragment evidence and composable molecule-flow transitions for Opus Magnum Codex.",
  "identity": {
    "puzzleId": "puz-<first16 sha256>",
    "solutionId": "sol-<first16 sha256>",
    "canonicalStructuralHash": "translation/rotation invariant hash preserving program timing",
    "canonicalMechanismHash": "translation/rotation invariant hash with normalized program timing",
    "puzzleFeatureFingerprint": "hash of solver-relevant puzzle chemistry, molecule topology, available parts and production constraints"
  },
  "replay": {
    "schemaVersion": "0.7.0",
    "glyphSimulation": {
      "supported": ["bonder", "unbonder", "glyph-calcification"],
      "eventKind": "glyph-effect",
      "effects": ["bond-created", "bond-removed", "calcify"]
    }
  },
  "fragmentIndex": {
    "purpose": "Catalog reusable local functional sub-mechanisms extracted from historical solutions.",
    "roles": ["feed", "output", "bonding", "conversion", "disposal", "conduit", "process"],
    "grouping": ["role", "canonicalMechanismHash"],
    "evidenceLevels": ["dynamic-confirmed", "dynamic-arm-observed", "structural-only"]
  },
  "fragmentFlowIndex": {
    "purpose": "Catalog replay-observed composition transitions between canonical functional fragment mechanisms.",
    "lineage": "Atom ids persist across replay molecule merge/split operations and are used as lineage markers.",
    "grouping": ["sourceRole", "sourceMechanismHash", "targetRole", "targetMechanismHash", "relation"],
    "relations": ["bond-created", "bond-removed", "calcify", "delivered", "consumed"],
    "transitionFields": {
      "observationCount": "integer",
      "sourcePuzzleCount": "integer",
      "sourceSolutionCount": "integer",
      "sourcePuzzles": "string[]",
      "samples": "source solution references and observed cycle ranges"
    },
    "limitations": [
      "Only replay-observed functional effects produce flow edges.",
      "Unsupported glyphs do not yet contribute transformation transitions.",
      "Arm movement is modeled as transport, not as a functional flow node.",
      "Collision correctness and exact glyph semantics still require OMSim validation."
    ]
  }
}
